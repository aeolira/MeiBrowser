<Window x:Class="GUI.StartDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        mc:Ignorable="d"
        Background="#1E1E1E"
        Title="StartDialog" Height="600" Width="600">

    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="140"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="75"/>
            </Grid.ColumnDefinitions>

            <!-- Mode -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Mode:" VerticalAlignment="Center"/>
            <ComboBox Margin="15" x:Name="ModeCombo" Grid.Column="1" Grid.Row="0" Height="30"
                SelectionChanged="ModeCombo_SelectionChanged" />

            <Button x:Name="ModeHelpButton" Grid.Row="0" Grid.Column="2" Content="Infos" Height="32" Width="75"
                HorizontalAlignment="Right" Click="ModeHelpButton_Click"  Background="#2A2A2A"/>

            <!-- Game -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Game:" VerticalAlignment="Center"/>
            <ComboBox Margin="15" Grid.Row="1" Grid.Column="1" x:Name="GameCombo" Height="30" IsEnabled="False" SelectionChanged="GameCombo_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding Icon}" Width="24" Height="24" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <!-- Server -->
            <TextBlock x:Name="ServerTitle" Grid.Row="2" Grid.Column="0" Text="Region:" VerticalAlignment="Center"/>
            <ComboBox Margin="15" x:Name="ServerCombo" Grid.Column="1" Grid.Row="2" Height="30"
                    IsEnabled="False"
                    SelectionChanged="ServerCombo_SelectionChanged" />

            <!-- Custom sophon -->
            <TextBlock x:Name="CustomSophonTitle" Visibility="Hidden" Grid.Row="2" Grid.Column="0" Text="URL:" VerticalAlignment="Center"/>
            <TextBox Visibility="Hidden" x:Name="CustomSophonUrl" Margin="15" Grid.Column="1" Grid.Row="2" Height="30" Background="#2A2A2A" Foreground="White" BorderThickness="1"/>
            <Button Visibility="Hidden" x:Name="CheckSophonButton" Grid.Row="2" Grid.Column="2" Content="Check" Height="32" Width="75"
                HorizontalAlignment="Right" Click="CheckSophonButton_Click"  Background="#2A2A2A"/>
            
            <!-- Version -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Version:" VerticalAlignment="Center"/>
            <ComboBox Margin="15" x:Name="VersionCombo" Grid.Column="1" Grid.Row="3" Height="30"
                    IsEnabled="False"
                    SelectionChanged="VersionCombo_SelectionChanged"/>

            <!-- Category -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Package:" VerticalAlignment="Top" Margin="0,5,0,0"/>
            <ListBox Margin="15" x:Name="CategoryListBox" Grid.Column="1" Grid.Row="4"
                    IsEnabled="False"
                    Background="#2A2A2A" Foreground="White" BorderThickness="1"
                    SelectionMode="Multiple">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                  Foreground="White" Margin="4,3"
                                  Checked="CategoryCheckBox_Changed"
                                  Unchecked="CategoryCheckBox_Changed"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Diff mode -->
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Diff Mode:" VerticalAlignment="Center"/>
            <CheckBox x:Name="DiffMode" IsEnabled="False" Margin="15" Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" Content="Only show new/changed files from previous version" IsThreeState="False"/>

            <Button x:Name="DownloadAllButton" IsEnabled="False" Grid.Row="6" Grid.Column="0" Content="Download All" Height="32" Width="110"
                HorizontalAlignment="Left" Click="DownloadAllButton_Click" Background="#2A2A2A"/>

            <Button x:Name="STokenButton" Grid.Row="6" Grid.Column="1" Content="Custom Build" Height="32" Width="100"
                HorizontalAlignment="Center" Click="STokenButton_Click" Background="#2A2A2A"/>

            <Button x:Name="PushMotrixButton" IsEnabled="False" Grid.Row="6" Grid.Column="1" Content="Motrix" Height="32" Width="65"
                HorizontalAlignment="Left" Margin="105,0,0,0" Click="PushMotrixButton_Click" Background="#2A2A2A"/>

            <Button x:Name="ConfirmButton" IsEnabled="False" Grid.Row="6" Grid.Column="2" Content="Confirm" Height="32" Width="100"
                HorizontalAlignment="Right" Click="Confirm_Click" Background="#2A2A2A"/>
        </Grid>

        <Grid x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="LoadingText" Text="Loading..." Foreground="White" FontSize="16" HorizontalAlignment="Center"/>
                <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Width="300" Height="20" Margin="0,5,0,0"/>
                <TextBlock x:Name="LoadingDetailText" Text="" Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
